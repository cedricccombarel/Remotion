Place a Boom Bap Hip-Hop loop here named `boom-bap-loop.mp3` (or update the
`src` in `src/GrooveGraffAncv.tsx`). The `<Audio loop>` component will play it
under the whole 10s video at 35% volume.

Use a track you're licensed to use commercially (e.g. from Epidemic Sound,
Artlist, or a royalty-free library) — no audio file is bundled here.
